version: 2.1

jobs:
  BILLY:
    docker:
      - image: cimg/base:current

    steps:
      - checkout
      - setup_remote_docker:
          version: default  
          docker_layer_caching: true
      - run:
          name: Build docker image
          command: | 
            docker build -t aquasupportemea/insecure-bank-app:${CIRCLE_SHA1} .
            docker image ls

workflows:
  build_and_scan:
    jobs:
      - BILLY
